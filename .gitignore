# R session artifacts
.Rhistory
.RData
.Ruserdata
.Rapp.history

# RStudio project settings (user-specific)
.Rproj.user/

# renv: track the lockfile and activate script, ignore the cache
renv/library/
renv/staging/
renv/python/
renv/sandbox/
renv/.cache/
# keep: renv/activate.R and renv/settings.json (committed)

# Package build artifacts
*.tar.gz
*.zip
*/src/*.o
*/src/*.so
*/src/*.dll

# Documentation build output
*/docs/
*/man/*.Rd      # if using roxygen2, these are generated

# Test snapshots (optional — remove if you want to track them)
# */tests/testthat/_snaps/

# OS files
.DS_Store
Thumbs.db
